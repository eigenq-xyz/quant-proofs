# Licensed / downloaded data — NEVER commit (repo hard rule: no WRDS/OptionMetrics in git)
data/
*.parquet
*.csv
*.zip

# Ad-hoc CLI run artifacts (curated study outputs live under studies/)
results/

# Lean / Python build + cache
.lake/
build/
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
.venv/
