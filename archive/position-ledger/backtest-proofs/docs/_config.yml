title: backtest-proofs
author: Akhil Karra

execute:
  execute_notebooks: "off"

latex:
  latex_documents:
    targetname: book.tex

bibtex_bibfiles:
  - references.bib

repository:
  url: https://github.com/eigenq-xyz/quant-proofs
  path_to_book: backtest-proofs/docs
  branch: main

html:
  use_issues_button: true
  use_repository_button: true
  extra_navbar: "Educational use only — not for production trading"

sphinx:
  config:
    html_theme: quantecon_book_theme
    myst_heading_anchors: 4
